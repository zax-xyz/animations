import Campaigns from 'Campaigns';
import 'twin.macro';

const App = () => (
  <div tw="flex min-h-screen bg-gray-50">
    <div tw="flex items-center w-full max-w-7xl p-12 mx-auto">
      <Campaigns />
    </div>
  </div>
);

export default App;
